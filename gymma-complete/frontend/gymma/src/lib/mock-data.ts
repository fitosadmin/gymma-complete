// Real Bengaluru gym data now lives in gyms.generated.ts (sourced from Google
// Places). Re-exported here so every existing import path keeps working.
export { MOCK_GYMS } from "@/lib/gyms.generated";
